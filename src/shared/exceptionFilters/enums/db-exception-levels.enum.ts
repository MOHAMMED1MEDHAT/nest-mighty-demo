export enum DBExceptionTypes {
	EXISTS = 'exists',
	NOT_FOUND = 'not found',
}

export enum DBExceptionMsgsEn {
	EXISTS = 'This record already exists in the database',
	NOT_FOUND = 'This record does not exist in the database',
}

export enum DBExceptionMsgsAr {
	EXISTS = 'هذا المحتوي موجود بالفعل',
	NOT_FOUND = 'هذا المحتوى غير موجود',
}
